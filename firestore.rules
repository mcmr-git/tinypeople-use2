rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /useCases/{docId} {
      // Anyone can read
      allow read: if true;
      // Only authenticated users can write (admin check done client-side + server rules)
      allow write: if request.auth != null;
    }
  }
}
